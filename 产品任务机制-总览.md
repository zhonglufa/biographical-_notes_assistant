# 自主任务机制 · 总览（Overview）

> 依据用户诉求建立：「我没空搭理你 → 知道的事情直接干，不知道的事情问我并完善成规则；涉及业务逻辑的多问；行业标准要去遵守。」
> 提交：`a096c99`；pre-commit 三闸门（版本戳 / 契约 66·6 / PRD-HLD v4.5）全绿。机制文件已纳入 git（本地，未 push）。

---

## 一、机制长什么样（五阶段流水线）

```
① 定时触发 ──▶ ② 分发任务 ──▶ ③ 执行任务 ──▶ ④ 状态回传 ──▶ ⑤ 日志与告警
  (cron       (TASK-QUEUE     (按 R1–R4      (追加 TASK-LOG   (全量日志 +
   :00/:20/:40 + .task-claims 决策+双闸门     + 回写队列/      TASK-ALERTS
   自动唤醒)   锁防重复)       +本地提交)       PROJECT_BRAIN)   待决项)
```

- **① 定时触发**：3 条错峰自动化（每 20 分钟），独立会话、靠文件维持连续性。
- **② 分发**：从 `TASK-QUEUE.md` 取队首「待办」；用 `.task-claims.json` 认领锁防 3 条重复认领。无待办 → 记「无待办」结束（**不伪造工作**）。
- **③ 执行**：按决策策略 R1–R4 推进；过 REVIEW 三闸门（双闸门 CI / 自审 / 红线禁自动提交）；本地 commit。
- **④ 状态回传**：纯写文件 —— 追加 `TASK-LOG.md`、回写 `TASK-QUEUE.md` 状态、`PROJECT_BRAIN.md` §2/§7。**不打扰用户**。
- **⑤ 日志与告警**：运行全量留痕；凡阻塞/需拍板 → 落 `TASK-ALERTS.md`，等你集中查看。

**查询入口**：`python scripts/task_status.py queue|log|alerts|health`（零依赖）。`health` 当前：3 个机制文件齐备、待办 0 / 阻塞 6 / 待拍板 6。

---

## 二、决策策略（四规则，已写进跨项目记忆）

| 规则 | 含义 | 落到哪 |
|---|---|---|
| **R1 知道→干** | 自驱区（技术/架构/代码/测试/文档/CI/调研/套用已固化规则）自主推进，不每步问 | 直接执行 |
| **R2 不知道→问→完善成规则** | 未知/歧义 → 停下问用户 → 回答后立即沉淀为规则，下次转 R1 | `TASK-ALERTS.md` + `MEMORY.md` |
| **R3 业务逻辑→多问** | 商业模式/定价/用户定位/KPI阈值/业务优先级 → **询问区，绝不拍板** | `TASK-ALERTS.md` |
| **R4 行业标准→遵守** | PIPL/招聘平台ToS/LLM治理/安全/WCAG/软件工程标准强制；冲突时标准优先 | 实现 + 告警标注 |

**询问区（只记录、不拍板、不伪造）**：业务逻辑、合规解释、花钱、物理动作、未知歧义。
**自驱区（R1）**：技术实现、工程取舍、测试、文档、调研、套用规则。

---

## 三、行业标准合规清单（R4 强制）

- **PIPL**：PII 最小化/同意/crypto-shred/DSAR 就绪（⏸ 随 D 阶段延后，报告标「用户延后」）
- **招聘平台服务条款**：半自动+用户确认、限频、禁凭据抓取（✅ 设计已含确认闸门/限额/幂等/验证码暂停）
- **LLM 治理**：成本硬上限+熔断、公开服务内容标识、Prompt 脱敏（✅ 护栏2 已落地）
- **信息安全**：密钥入 vault、不落明文、最小权限、OAuth2（✅ 密钥工程 LLD）
- **无障碍 WCAG 2.1 AA**：原型已尊重 reduced-motion；§19 完整 AA 待定（A5）
- **软件工程**：SemVer 三处版本戳一致、12-Factor、契约优先、双闸门 CI、TDD

---

## 四、诚实边界（永不可越）
- 绝不伪造「完成」；阻塞/未知 → 写告警、标 BLOCKED。
- 物理动作（部署/真实凭据/上线/处理真实 PII）与 PIPL 上线前签署 **仅用户可做**，标「待用户触发」。
- 循环上限 = 生产就绪的设计+代码骨架+UI稿+mock 测试，**止于未上线**。

---

## 五、当前待你拍板（R3 多问落点 · `TASK-ALERTS.md` A1–A6）

| ID | 业务逻辑问题（需你定义，我不替你拍板） |
|---|---|
| A1 | 商业模式/定价：免费？订阅？会员分层与收费点？ |
| A2 | 目标用户画像/市场定位：应届/社招/行业细分？ |
| A3 | 「成功」KPI 阈值（业务风险偏好）：可接受封号率？日投递量？留存？ |
| A4 | 阶段二 RAG 优先级与范围（面试/求职建议/匹配） |
| A5 | 无障碍 WCAG 2.1 AA 完整落地是否补做（非事故项） |
| A6 | 接入真实招聘平台（触发前提：你提供真实账号·API 密钥 → 物理动作 Q6） |

你回答任意一条，我会**立即把结论沉淀成规则**（更新 `MEMORY.md`/`TASK-MECHANISM.md`），之后同类问题走 R1 不再问。

---

## 六、如何启用循环
当前 3 条自动化 **PAUSED**（resume /goal 已达成，无待办）。当你想让它自主续推：
1. 向 `TASK-QUEUE.md` 投放新任务（加一行「待办」）；
2. 把 3 条自动化状态由 `PAUSED` 改为 `ACTIVE`（或让我代开）；
3. 循环会按五阶段自动认领、执行、回传、告警。

---

## 七、已交付文件
- `TASK-MECHANISM.md` —— 规则手册（权威）
- `TASK-QUEUE.md` / `TASK-ALERTS.md` / `TASK-LOG.md` —— 队列/待决/日志
- `scripts/task_status.py` —— 状态 CLI
- `.task-claims.json` —— 认领锁
- `PROJECT_BRAIN.md` §6 文档地图 + §10 机制指针
- `~/.workbuddy/MEMORY.md` —— 跨项目决策策略 R1–R4 已固化
