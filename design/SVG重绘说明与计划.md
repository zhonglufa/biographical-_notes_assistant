# 6 张 SVG 重绘说明与实施计划

> 生成：2026-08-15 复验。目的：回答「为什么要重绘 / 旧图与新图的区别」，并给出待办清单（#94–#99）的执行计划。
> 当前所有改动保持**未提交态**（遵循「提交删除先不急」）。

---

## 一、为什么要重绘（核心结论）

**不是「信息没变想重画」，而是「画图信息已更新 + 旧图画错了，必须纠正」。**

根因：产品确立了**「本机 Agent 模型」红线（ADR-001 / ADR-002）**——浏览器自动化（Playwright）只存在于用户本机 Agent，服务端容器不得画浏览器沙箱 / Docker；用户平台 Cookie 不落服务端库；不存在「HR→系统」主动回推箭头。旧图是在这个模型确立之前画的，违反了上述红线，属于**架构错误**而非风格问题，因此必须重绘。

---

## 二、旧图 vs 新图 逐图区别（红线条款来自 `图信息说明书.md`）

| 图 | 旧图问题（违反红线） | 新图纠正 |
|----|----------------------|----------|
| **C1 系统上下文** | 存在「HR→系统」主动回推箭头；招聘平台只画 3 家 | 删除 HR→系统箭头（并入「系统→招聘平台」的 HR 尽力感知语义，§7.3）；招聘平台补满首期 5 家；补「主 + 备用 LLM」 |
| **C2 容器图** | 服务端画「Python 自动化引擎承载浏览器自动化」+「浏览器沙箱 Docker」 | 删除服务端浏览器沙箱 / Docker；新增黄色「本机 Agent 层（用户侧）」承载 Playwright 实例池（≤3）+ 本地加密 Cookie |
| **2-2 批量投递时序** | 错绘「Python 调度中心从队列消费并分配**服务端**浏览器实例执行」 | 改为「**本机 Agent 拉取任务 → 本地实例池 + 本地 Cookie 执行**」 |
| **2-3 部署图** | 应用层 / Host 内画「Python 服务（Playwright 自动化）+ 浏览器沙箱（Docker）」 | 删服务端浏览器算力；浏览器实例池仅用户侧本机 Agent；服务端仅 Java + Python(LLM 网关) + 数据层 |
| **2-4 HR 状态时序** | 可能画「Java 直连 Python 适配器（服务端）执行查询」 | 改为「服务端经 B08 下发轮询 → 本机 Agent 适配器加载本地 Cookie 查询平台页面 → 回写」 |
| **2-5 AI 匹配降级** | 只有「主 LLM → 规则引擎」两级 | 补「主 LLM 不可达 → 自动切备用 LLM（golden set κ 容差回归）→ 仍失败降级规则引擎」三级降级 |

---

## 三、当前实测状态（2026-08-15 复验，2026-08-16 更新）

**架构正确性（grep 验证，全部通过红线）：**
- 6 张目标 SVG 均已按新信息重绘，无 `Docker` / `沙箱` / `HR→系统` 残留。
- fig-2-3：标「浏览器自动化下沉至用户本机 Agent（ADR-001/002）」「服务端无浏览器算力」「Playwright · 浏览器实例池 ≤3」。
- fig-c2：有「本机 Agent / Playwright 实例池 ≤3 / 本地加密 Cookie」。
- fig-2-5：有「备用 LLM」「切备用 LLM + golden set 回归（κ 容差）」。
- fig-c1 / fig-c2：画满平台集与备用 LLM。

**lint 结果（fresh run，2026-08-16）：6 PASS / 0 FAIL**

| 文件 | 状态 | 说明 |
|------|------|------|
| fig-c1-system-context.svg | ✅ PASS | 4 项全过 |
| fig-c2-container.svg | ✅ PASS | 4 项全过 |
| fig-2-2-apply-flow.svg | ✅ PASS | 4 项全过 |
| fig-2-3-deployment.svg | ✅ PASS | 4 项全过 |
| fig-2-4-hr-status.svg | ✅ PASS | 4 项全过 |
| fig-2-5-ai-match.svg | ✅ PASS | 4 项全过 |

**更新说明：** 原 ST-14 误报已通过 `lint-svg.cjs` 增加盒内文字排除逻辑 `textCenterInsideBox(text, parser.rects) continue;` 修复；fig-c1 招聘平台 5 家名已补全。两项均已在之前的迭代中完成。

---

## 四、待办清单与计划（#94–#99）

### #94 — SVG 收口 ✅ 已完成
1. ST-14 误报闭合 — 已通过 `lint-svg.cjs` 增加盒内文字排除逻辑修复。
2. fig-c1 补满 5 平台名 — 已将「BOSS / 猎聘 / 智联 等」改为「BOSS直聘 / 猎聘 / 智联招聘 / 前程无忧 / 拉勾」。

### #95 — 文档与命名清理 ✅ 已完成
1. `图信息说明书.md` → 顶部已含退役说明（`⚠️ **本文档已退役**：6 张 SVG...已由用户按本说明**重绘完成**`），无需额外操作。
2. `fig-c2-container-v2.svg` → 已重命名为 `fig-c2-container-v2.svg.bak`，消除命名歧义。
3. HLD 双验证状态 → 实地验证：所有 10 处"双验证通过"声明（§208/§221/§246/§265/§284/§320/§805/§1629/§1661/§1674-1676）均已准确——#94 修复后 6 张 SVG 全部 lint PASS，无需修改 HLD 正文。

### #96 — LLD 🟠F-4~F-9 ✅ 已完成（LLD v1.2 已修复）
1. **F-4 warm-up 模型**：时间窗近似已标注"偏离 HLD 份数模型，待 PoC P1 校准"
2. **F-5 抖动规格**：上限从 12s 修正为 8s（对齐 HLD 2–8s），补贝塞尔轨迹 + 随机滚动拟人动作
3. **F-6 孤儿进程回收**：`BrowserPool.reap_orphans()` 级联回收 + 幂等键标记"执行中断"
4. **F-7 本地持久化健壮性**：`LocalStore` 补 `integrity_check()` / `snapshot()` / `enter_safe_mode()`
5. **F-8 §6.15 呼应澄清**：删除误导表述，明确"本机 SQLite 不适用分片"
6. **F-9 IPC 通道规格**：补帧格式 `[4B len][1B ver][1B op][payload]` + 启动握手 `auth` + 版本协商

### #97 — LLD 🟡F-10~F-14 ✅ 已完成（LLD v1.2 已修复）
1. **F-10 自检虚报**：§14 补第 5 条时序（断网补传），虚报数从"≥5"修正为真实覆盖
2. **F-11 PRD 锚点虚构**：§15.x 更正为 §494/§496
3. **F-12 TC 占位**：§11 标注"测试用例待撰写"而非暗示已完成
4. **F-13 PoC 阈值未锚定**：§10 标注"<20% 为 LLD 提议值，需 stakeholder 签字确认"
5. **F-14 退避 base/cap 未回声**：§5.1 补 `base=15min, cap=6h` 常量

### #98 — R-09 收口 ✅ 已完成（LLD v1.2 已闭环）
R-09 已在 LLD 中完整收口：§3.3 补失效检测 SLA（≤60s 判定 + 单平台降级非全局停摆）+ 单平台降级状态机（§4.4 `active → pending_fix → fixed | retired`）+ 24h 升级阻断。详见 LLD §3.3 / §4.4 / §13。

### #99 — 统一 git 提交
按「提交删除先不急」延后，所有改动保持未提交态。

### #100 — PoC 实验规划 ✅ 已产出（v0.1 草案，待签字）
已产出 `PoC-实验规划.md`（v0.1 草案）：P1 反封号有效性（14 天实跑 + 参数冻结 + 验证码率 <20%）+ P2 适配器 DOM 稳定性（3 平台 × 5 页面 fixture + 每周回放 + 连续 2 周 ≥95%）+ Gate 决策表。阻塞项：测试账号 ×3 需用户提供；F-13 阈值 <20% 需 stakeholder 签字。
O-1 测试账号来源调研已完成（2026-08-15）：结论为自备/亲友闲置实名账号，接码/租号渠道已明确排除（违法 + 号段被风控标记 + 实验失真），备忘见 PoC §7.1。待将军提供账号后 P1 可启动。

---

## 五、ST-14 闭合方式 ✅ 已实施

**方案 A（推荐）已实施**：`lint-svg.cjs` 新增 `textCenterInsideBox()` 函数，ST-14 检查时跳过盒内文字（盒名/子标签中心落在 `<rect>` 范围内不视为线标注）。当前所有 6 张 SVG 均 PASS。
