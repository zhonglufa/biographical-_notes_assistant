# 自主任务机制（Task Mechanism）· 操作规则手册

> **用途**：让用户「没空搭理」时，AI 能自主、安全、可追溯地持续推进工作；把协作方式**完善成可复用的规则**。
> **适用**：本仓库的定时自主循环（`automation-1786905748859` 主 + `...104193`/:20 + `...104563`/:40 错峰，每 20 分钟一轮）。规则本身跨项目继承（见 `~/.workbuddy/MEMORY.md`）。
> **一句话原则**：**知道的事情直接干，不知道的事情问（并沉淀成规则），业务逻辑的要多问，行业标准必须遵守。**

---

## 0. 五阶段流水线（定时触发 → 分发 → 执行 → 状态回传 → 日志与告警）

每个 tick 严格走完 5 个阶段，缺任一段都视为该轮不完整、须在日志标注。

| 阶段 | 名称 | 干什么 | 产物 / 落点 |
|---|---|---|---|
| ① | **定时触发** | 自动化 cron 唤醒（:00/:20/:40），开全新会话 | 读 `PROJECT_BRAIN.md` §2/§7 + 当日日志末条，做续做自检 |
| ② | **分发任务** | 从 `TASK-QUEUE.md` 取队首未完成项；经认领锁防重复认领 | 更新 `TASK-QUEUE.md` 状态为 `进行中`；写认领锁 |
| ③ | **执行任务** | 按【决策策略 R1–R4】决定「干 / 问 / 沉淀」；产出文件、跑双闸门、本地提交 | 业务/代码/文档产物 + git 本地提交 |
| ④ | **状态回传** | 本轮做了什么、结果如何，**被动写文件**（不打扰用户） | 追加 `TASK-LOG.md` 一条；回写 `PROJECT_BRAIN.md` §2/§7；更新 `TASK-QUEUE.md` |
| ⑤ | **日志与告警** | 全量运行留痕；凡遇阻塞/需用户拍板，**落 `TASK-ALERTS.md`** 而非臆测 | `TASK-LOG.md`（追加式全量）+ `TASK-ALERTS.md`（待决项） |

- **查询入口**：`python scripts/task_status.py [queue|log|alerts|health]`（零依赖），人机可读地回看 ②③④⑤。
- **不打扰承诺**：阶段 ④⑤ 纯写文件，不弹窗、不每轮问；仅当 `TASK-ALERTS.md` 新增条目（真正需要你拍板）时，在你有空查看时集中呈现。

---

## 1. 决策策略（核心四规则）

### R1 · 知道 → 直接干
- 落在【自驱区】且信息充分、不越权的事项，**自主规划→调研→执行→出报告**，不每步追问、不用选择题问「按哪个范围」。
- 执行纪律：先内部任务清单再动手；高风险设计主动多轮审计 + 缺口登记（风险登记表），不把「未覆盖」假装成「已覆盖」。

### R2 · 不知道 → 问 → 完善成规则
- 遇到**未知 /  ambiguous** 事项：**先停下来问用户**（写入 `TASK-ALERTS.md` 待决项，本轮不臆测）。
- 用户回答后，**立即把结论沉淀进规则**（更新 `~/.workbuddy/MEMORY.md` 或本手册），使下一次同类问题变成「已知 → 走 R1」。
- 目的：让「问」一次性发生，之后自动化，符合「完善成规则」。

### R3 · 业务逻辑 → 多问（不问就别动）
- 凡属**业务逻辑 / 商业取舍 / 产品定义**的决策，一律**询问区**，绝不替用户拍板：
  - 商业模式 / 定价 / 会员分层 / 收费点
  - 目标用户画像 / 市场定位
  - 「成功」的定义与 KPI 阈值（如可接受封号率、可达投递量、留存目标）
  - 功能优先级 / 范围裁剪的**业务**理由
- 实现层面（技术架构、如何落地、测试、文档）仍走 R1 自驱；但「做哪个、为谁做、值不值」属 R3。

### R4 · 行业标准 → 遵守
- 适用行业标准**强制执行**；当捷径与标准冲突时，**标准优先**，并在告警中说明阻塞点。
- 本产品须遵守的标准清单见 §3；通用软件工程标准见 §4。
- 若某标准因用户决策暂缓（如 PIPL 合规随 D 阶段延后），须**如实标注「用户延后」**，不伪造「已合规」。

---

## 2. 边界：询问区 vs 自驱区

### 自驱区（R1，直接干）
- 技术架构、系统设计与模块拆分
- 代码实现、测试（单元/集成/契约回归）、文档撰写
- CI/CD 脚手架、监控接入代码（不真部署）
- 调研、事实核查、信息综合
- **套用已固化规则**执行
- 在已确立标准内的工程取舍

### 询问区（R2/R3，先问后动）
- **业务逻辑**（R3）：商业模式 / 定价 / 用户定位 / 成功口径 / 业务优先级
- **合规红线的解释与接受度**：哪些红线可接受、接受程度（注：PIPL/法检随 D 阶段延后，本次一般不再触碰，但仍属询问区）
- **花钱 / 采购 / 订阅 / 付费 API / 付费凭据**
- **物理动作**：部署生产、提供真实账号·API 密钥、点上线开关、处理真实用户 PII、PIPL 上线前签署
- **未知 / 歧义**事项（R2）

> 询问区事项在本轮**只记录、不拍板、不伪造**；落到 `TASK-ALERTS.md`，等用户有空时一次性回答。

---

## 3. 本产品须遵守的行业标准（R4）

| 标准 | 范围 | 本仓库落实状态 |
|---|---|---|
| **PIPL 个人信息保护法** | PII 最小化收集、明示同意、加密存储(crypto-shred)、DSAR 删除权就绪 | 🔄 2026-08-18 定位真上线 → **必须补齐(R4)**：设计/文档/代码基座纳入 Q3，法定签署(Q7)仍仅用户触发 |
| **招聘平台服务条款**（Boss直聘/猎聘等） | 半自动 + 用户显式确认；限频；禁止凭据抓取/模拟登录滥用 | ✅ 设计已含「人工确认闸门 + 日限额 + 幂等 + 验证码暂停」降险 |
| **LLM 治理**（生成式 AI 合规） | 成本硬上限 + 熔断；若公开服务需内容标识；Prompt 不传真实 PII | ✅ 护栏2 `CostGuard`/`BudgetPolicy` 已落地；Prompt 脱敏约定 |
| **信息安全**（ secrets 管理） | 密钥入 vault、不落明文、最小权限、OAuth2 标准 | ✅ 密钥工程 LLD 已设计；本地不存真实凭据 |
| **无障碍 WCAG 2.1 AA** | 可感知/可操作/可理解/健壮；`prefers-reduced-motion` | 🔄 原型已尊重 reduced-motion；§19 完整 AA 待定（非事故相关） |
| **等保 2.0**（若规模化处理 PII） | 安全等级保护测评 | 🔄 真上线规模化处理 PII 时必须；纳入合规待办 Q3 |

---

## 4. 通用软件工程标准（R4，强制）

- **语义化版本 SemVer**：文档/契约版本戳头/脚/修订记录三处一致（双闸门不查此项，但交付前须核对）。
- **12-Factor / 配置外置**：密钥、阈值、URL 不硬编码。
- **契约优先 Contract-First**：A 层 25 端点(A01–A25) `fully-detailed`，先契约后实现。
- **双闸门 CI**（护栏1）：`validate_contracts.py`（66 schema/6 registry）+ `check_prd_hld_traceability.py`（PRD↔HLD v4.5），提交前必绿。
- **TDD / 测试闭环**：功能(T1)/集成(T2)/契约回归(T3) 全绿方可称「测试通过」。
- **可回滚**：只本地 commit、绝不自动 push；CD 须 `DEPLOY_TOKEN` 门控。

---

## 5. 诚实边界（永不可越）

- **绝不伪造「完成」**：阻塞/未知 → 写告警、标 BLOCKED，**不假装已覆盖**。
- 物理动作（部署 / 真实凭据 / 上线 / 处理真实 PII）与 PIPL 上线前法定签署 **仅用户可做**，报告标「待用户触发」。
- 循环到「生产就绪的设计 + 代码骨架 + UI 稿 + mock 测试」即上限，**止于未上线**；上线闭环须用户介入。

---

## 6. 与现有自动化的映射

- 3 条错峰自动化（每 20 分钟）即阶段①的「定时触发」；其 prompt 已改写为本手册的 5 阶段 + R1–R4。
- 阶段②认领锁沿用 `design/ui/.u-claims.json`（防 3 条重复认领）。
- 阶段④⑤产物：`TASK-QUEUE.md` / `TASK-LOG.md` / `TASK-ALERTS.md`（本仓库根目录）。
- 复用 `PROJECT_BRAIN.md` 作主状态源；`scripts/task_status.py` 作查询入口。
- 当前 `resume-ai-prod` 的 /goal（A+B+C+U+V+T+O + 护栏1/2/3）已 **GOAL REACHED**；循环 **ACTIVE**（3 条错峰自动化），按 `TASK-QUEUE.md` 认领续推。2026-08-18 战略定位=**真上线产品**：V3 屏幕 Q10–Q15 + 合规护栏 Q2–Q4 全部纳入循环范围，Q5–Q7 物理动作待你触发。

---

## 7. 启用 / 查看方式

- **查看状态**：`python scripts/task_status.py queue` / `log` / `alerts` / `health`
- **启用循环**：把 3 条自动化状态由 `PAUSED` 改 `ACTIVE`（或新增任务后由用户触发）。
- **提交待决（R2/R3）**：在 `TASK-ALERTS.md` 加一条，下轮循环读到即停下对应包、等你拍板；你回答后我沉淀规则、闭环。

---

## 8. 已沉淀战略决策（R2 闭环）

| 日期 | 决策（用户拍板后即时沉淀） | 影响范围 |
|---|---|---|
| 2026-08-18 | **定位=真上线产品 + 原型全做**：V3 屏幕 Q10–Q15 一步不能少全部转化；Q2–Q4 合规护栏(R4)解锁为待办由循环 R1 备基座；Q5–Q7 物理动作仍仅用户触发但属上线必做 | 循环续推范围扩大至合规；A1–A6 业务决策升级为上线前置，仍须你拍板 |
| 2026-08-18 | **前端技术栈 = Vue 3 + Element Plus（严守 ADR-010）**：`frontend/` 曾误用 React18+Vite5+react-router6，与 ADR-010 / HLD §2.4（Vue3+Element Plus PC + uni-app 多端）不符，属未记录技术栈偏离；用户拍板**回退 Vue3**，Q8–Q11 已完成的 React 屏重写为 Vue SFC + vue-router + Element Plus，Q12–Q15 按 Vue 落地，uni-app 多端维持原规划 | frontend 全部组件技术栈；V 阶段 Q12–Q15 续推方向 |
